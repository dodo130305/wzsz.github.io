# Vibe Coding 个人主页项目 — 过程日志

> 本日志记录项目执行过程中每一次任务的主要信息，用于项目结束后生成报告。
> 创建时间：2026-09-10

---

## 日志记录规范

每次任务记录包含：
- **日期时间**：任务执行时间
- **阶段/版本**：当前所处阶段（V1/V2/V3/V4/扩展）
- **任务内容**：本次任务的目标和范围
- **主要变更**：具体做了什么修改
- **遇到的问题**：遇到的困难及解决方案
- **验证结果**：修改后的验证情况
- **下一步计划**：后续待办事项

---

## 任务 #001 — 项目初始化与大纲理解

**日期时间**：2026-09-10
**阶段/版本**：项目启动（V1 前期）
**任务内容**：
1. 阅读课程大纲《项目制 Vibe Coding 入门模块》
2. 充分理解项目 scope 和各阶段要求
3. 在项目文件夹内创建理解文档作为后续参考
4. 建立过程记录机制

**主要变更**：
- 读取并解析课程大纲 PDF（共 380.1 KB）
- 创建 `PROJECT_UNDERSTANDING.md` — 项目理解与参考文档，包含：
  - 项目整体目标与四句话原则
  - 课程范围与评分占比（总计 20%）
  - 各阶段（V1-V4）详细要求与最低成果检查项
  - 期末扩展与独立小作业要求
  - 过程证据要求
  - AI 使用与学术诚信规范
  - 当前项目状态分析
  - 技术栈与工具参考
  - 关键约束清单
- 创建 `PROJECT_LOG.md` — 本过程日志文件
- 检查现有项目文件：`index.html`、`avatar.jpg`、`bg.jpg`、`ref.jpg`

**当前项目状态分析**：
- 已有一个极早期雏形（全屏背景 + 底部文字"我这是在？" + 头像）
- 具备：全屏背景图、暗色渐变遮罩、顶部导航图标、入场动画、响应式适配
- 缺失：真实个人信息、个人简介/经历/项目、清晰页面结构、可交互入口、版本记录

**遇到的问题**：无

**验证结果**：
- 课程大纲完整读取，内容覆盖 4 个阶段 + 期末扩展 + 独立作业
- 理解文档已创建，结构完整
- 现有 index.html 可正常解析，共 239 行

**下一步计划**：
1. 等待用户提供真实个人信息（姓名、简介、经历、项目、联系方式等）
2. 基于现有雏形完善 V1 版本，建立清晰的页面结构
3. 初始化 Git 版本管理
4. 确认 V1 的目标受众和页面核心信息

---

## 任务 #002 — V1 版本制作：基于真实信息的个人主页

**日期时间**：2026-09-10
**阶段/版本**：阶段一 · V1
**任务内容**：
1. 基于用户提供的真实个人信息，完善 V1 版本个人主页
2. 建立清晰的页面结构，确保访问者能快速理解"你是谁、做过什么、下一步可以做什么"
3. 保留现有视觉素材（背景图、头像），融入真实内容
4. 本地预览验证

**用户提供的真实信息**：
- 姓名：刘子铖
- 身份：大学生
- 地点：广东广州
- 邮箱：dodo130305@outlook.com
- 兴趣：听歌、篮球、足球、打游戏
- 学校：深圳未来技术学院
- 专业：智能医学工程
- 年级：大一

**主要变更**：
- 完全重写 `index.html`（从 239 行社交资料页雏形 → 完整个人主页）
- 页面结构：
  - **Hero 全屏区域**：须佐能乎背景图 + 暗色渐变遮罩 + 姓名（刘子铖）+ 副标题（学校·专业·年级）+ 地点 + 二次元头像 + 滚动提示动画
  - **关于我卡片**：学校、专业、年级、所在地 四宫格信息展示
  - **兴趣爱好卡片**：听歌🎵、篮球🏀、足球⚽、打游戏🎮 四个标签
  - **联系我卡片**：邮箱链接（可点击跳转邮件客户端）
  - **页脚**：版本号 V1.0 + 版权信息
- 上传背景图和头像至 CDN 获取稳定 URL
- 设计风格：深色主题 + 毛玻璃卡片 + 渐变图标 + 悬停动效
- 响应式适配：移动端（<600px）、平板、桌面端（>1024px）三套布局
- 无障碍支持：尊重 `prefers-reduced-motion` 减少动画偏好

**遇到的问题**：
- 首次打开页面时背景图和头像未立即显示（CDN 加载延迟）
- 解决方案：刷新页面后正常加载，确认 URL 有效

**验证结果**：
- ✅ 可预览：网页在本地浏览器正常打开并完整显示
- ✅ 内容真实：所有个人信息均来自用户提供，无虚构内容
- ✅ 结构清楚：Hero（身份）→ 关于我（基本信息）→ 兴趣爱好 → 联系方式，访问者可快速理解
- ✅ 第一版记录：版本号 V1.0 已标注，过程日志已记录
- ✅ 响应式：桌面端显示正常，布局合理
- ✅ 图片加载：背景图、头像均正常显示
- ✅ 交互：邮箱链接可点击，卡片悬停有动效

**V1 最低成果检查项对照**：
| 检查项 | 状态 | 说明 |
|---|---|---|
| 可预览 | ✅ 通过 | 本地浏览器正常打开 |
| 内容真实 | ✅ 通过 | 全部信息来自用户提供 |
| 结构清楚 | ✅ 通过 | 四区块清晰布局 |
| 第一版记录 | ✅ 通过 | 版本号+日志已记录 |

**下一步计划**：
1. 用户确认 V1 效果，提出修改意见
2. 进入 V2 阶段：多轮小步打磨，参考公开设计提炼风格
3. 初始化 Git 版本管理，保存 V1 checkpoint
4. 可考虑补充：个人简介段落、项目经历展示（如有）、社交媒体链接

---

## 任务 #003 — 背景替换：静态图片 → 动态视频

**日期时间**：2026-09-10
**阶段/版本**：阶段一 · V1 迭代
**任务内容**：
用户上传了一个视频文件，要求将个人主页的背景从静态图片替换为该视频。

**用户上传的视频信息**：
- 原始文件：df69bdcc35eacb9bb9a7483a6fd7f558_raw.mp4
- 原始编码：H.265/HEVC
- 分辨率：1280×720
- 时长：3 秒
- 帧率：30fps
- 内容：素描风格动漫女孩画面，带"Reers ❤️"文字

**主要变更**：
1. **CSS 修改**：
   - 移除 `.hero` 的 `background-image` 静态背景图
   - 新增 `.bg-video` 样式：绝对定位、铺满全屏、`object-fit: cover`
   - 调整暗色渐变遮罩透明度（因视频为浅色背景，需增强遮罩确保白色文字可读）
   - 明确层级：视频 z-index:0 → 遮罩 z-index:1 → 内容 z-index:10

2. **HTML 修改**：
   - 在 `.hero` 区域内添加 `<video>` 元素
   - 属性设置：`autoplay muted loop playsinline`（自动播放、静音、循环、移动端内联播放）
   - 视频源指向转码后的 H.264 版本

3. **视频转码**：
   - 原始 H.265/HEVC 编码在 Chrome 浏览器中无法播放（显示黑屏）
   - 使用 ffmpeg 转码为 H.264 编码：`libx264 -crf 23 -preset medium`
   - 输出文件：`bg-video.mp4`（361KB，H.264 编码，兼容所有主流浏览器）
   - 转码后上传 CDN 获取稳定 URL

**遇到的问题**：
- **问题**：原始视频 H.265/HEVC 编码在浏览器中无法播放，背景显示为纯黑色
- **原因**：Chrome 对 HEVC 硬件解码支持有限，软件解码默认不启用
- **解决方案**：使用 ffmpeg 将视频转码为 H.264 编码，兼容性大幅提升
- **次要问题**：视频为浅色素描风格，白色文字可读性差
- **解决方案**：调整暗色渐变遮罩，整体增加不透明度（顶部 0.3→0.5，中部 0.1→0.3，底部保持 0.92）

**验证结果**：
- ✅ 视频背景正常播放，自动循环
- ✅ 视频静音，无音频干扰
- ✅ 白色文字在视频背景上清晰可读
- ✅ 头像、副标题、地点等元素正常显示
- ✅ 滚动提示动画正常
- ✅ 下方内容区域（关于我、兴趣爱好、联系方式）未受影响
- ✅ 移动端 playsinline 属性确保手机端内联播放

**技术要点记录**：
- 网页视频背景推荐使用 H.264 编码，避免 HEVC/VP9 等兼容性问题
- 必须设置 `muted` 才能在大多数浏览器中自动播放
- `playsinline` 属性确保 iOS Safari 内联播放，不进入全屏
- `loop` 属性实现无限循环
- 浅色视频背景需配合较深的遮罩层确保文字对比度

**下一步计划**：
1. 用户确认视频背景效果
2. 继续 V1 完善或进入 V2 多轮打磨阶段
3. 可考虑：添加视频加载失败时的静态图兜底（poster 属性）

---

## 任务 #004 — 重新执行背景更新并迁移到桌面文件夹

**日期时间**：2026-09-10
**阶段/版本**：阶段一 · V1 迭代
**任务内容**：
用户要求将更新背景的步骤重新完整执行一遍，并将所有项目文件保存到桌面的 personal-homepage 文件夹。

**背景**：
用户反馈在主目录文件夹中找不到更新后的文件，上传的 index.html 是旧的静态背景版本。为确保用户能直接访问到最新版本，决定在桌面创建独立的项目文件夹，并完整演示从静态图到视频背景的更新过程。

**执行步骤**：

### 步骤 1：创建桌面项目文件夹
- 创建路径：`/home/user/Desktop/personal-homepage/`
- 系统原无 Desktop 目录，自动创建

### 步骤 2：复制所有素材文件
从工作目录复制以下文件到桌面文件夹：
- `avatar.jpg`（头像，476KB）
- `bg.jpg`（原静态背景图，5.9MB）
- `bg-video.mp4`（视频背景，H.264编码，370KB）
- `ref.jpg`（参考图，190KB）
- `PROJECT_UNDERSTANDING.md`（项目理解文档）
- `PROJECT_LOG.md`（过程日志）

### 步骤 3：放入静态背景版本（更新前状态）
- 从用户上传的旧版 index.html 复制到桌面文件夹
- 该版本使用静态背景图：`background: url('https://aka.doubaocdn.com/s/SA7eeDJpVI')`
- 作为"更新前"的基准版本

### 步骤 4：执行背景更新（静态图 → 视频）
**CSS 修改**：
- 移除 `.hero` 的 `background-image` 静态背景图
- 将背景改为纯色 `background: #0a0a0f`
- 新增 `.bg-video` 样式：绝对定位、铺满全屏、`object-fit: cover`、`z-index: 0`
- 调整暗色渐变遮罩透明度（因视频为浅色背景）：
  - 顶部：0.3 → 0.5
  - 中部：0.1 → 0.3
  - 底部：保持 0.92
- 遮罩添加 `z-index: 1`，确保层级在视频之上、内容之下

**HTML 修改**：
- 在 `<section class="hero">` 内添加 `<video>` 元素
- 属性：`autoplay muted loop playsinline`
- 视频源：`https://aka.doubaocdn.com/s/LGb2hcgL3a`（H.264 转码版本）

### 步骤 5：验证
- CSS 验证：`.bg-video` 样式存在（第38行）
- HTML 验证：`<video>` 元素存在（第411行）
- 确认已无静态背景图（grep 返回 0）
- 浏览器预览验证：视频背景正常播放，文字清晰可读，头像正常显示

**遇到的问题**：
- 首次打开页面时视频显示为黑色（CDN加载延迟）
- 解决方案：刷新页面后正常播放
- 文件管理器无法自动打开（环境限制，无可用图形文件管理器）
- 解决方案：直接告知用户文件路径，手动导航

**最终桌面文件夹内容（7个文件）**：
| 文件 | 大小 | 说明 |
|---|---|---|
| index.html | 14,653 B | 个人主页（视频背景版本） |
| bg-video.mp4 | 370,060 B | 视频背景（H.264） |
| avatar.jpg | 476,731 B | 头像 |
| bg.jpg | 5,926,429 B | 原静态背景图（保留备用） |
| ref.jpg | 190,081 B | 参考图 |
| PROJECT_UNDERSTANDING.md | 7,922 B | 项目理解文档 |
| PROJECT_LOG.md | 8,258 B | 过程日志 |

**验证结果**：
- ✅ 视频背景正常播放、自动循环、静音
- ✅ 白色文字在视频背景上清晰可读
- ✅ 头像、副标题、地点等元素正常显示
- ✅ 所有7个文件齐全
- ✅ 文件保存在桌面文件夹：`/home/user/Desktop/personal-homepage/`

**下一步计划**：
1. 用户在桌面文件夹中打开 index.html 确认效果
2. 继续 V1 完善或进入 V2 多轮打磨阶段
3. 后续修改将同步更新桌面文件夹

---

## 任务 #005 — 确定统一保存位置：桌面「personal homepage完整最新版」文件夹

**日期时间**：2026-09-10
**阶段/版本**：项目管理
**任务内容**：
用户明确要求以后所有项目文件都统一保存在桌面的「personal homepage完整最新版」文件夹里。

**背景**：
之前文件分散保存在多个位置（工作目录 `.doubao/...`、主目录 `~/personal-homepage/`、桌面 `~/Desktop/personal-homepage/`），导致用户在自己设备上找不到最新版本，多次反馈"没有更新"。用户下载的旧文件夹中缺少 `bg-video.mp4`，index.html 也是旧的静态背景版本。

**执行步骤**：
1. 创建统一保存文件夹：`/home/user/Desktop/personal homepage完整最新版/`
2. 将所有最新文件从 `~/Desktop/personal-homepage/` 复制到新文件夹
3. 确认 7 个文件齐全
4. 记录用户偏好（preference_id: p1_AQCXgctvrRk），以后自动保存到此位置

**最终统一保存位置**：
```
/home/user/Desktop/personal homepage完整最新版/
```

**文件夹内容（7个文件）**：
| 文件 | 大小 | 说明 |
|---|---|---|
| index.html | 14,653 B | 个人主页（视频背景版本） |
| bg-video.mp4 | 370,060 B | 视频背景（H.264） |
| avatar.jpg | 476,731 B | 头像 |
| bg.jpg | 5,926,429 B | 原静态背景图（保留备用） |
| ref.jpg | 190,081 B | 参考图 |
| PROJECT_UNDERSTANDING.md | 7,922 B | 项目理解文档 |
| PROJECT_LOG.md | 11,811 B | 过程日志（已记录5次任务） |

**用户偏好已记录**：
- preference_id: p1_AQCXgctvrRk
- 内容：个人主页项目所有文件统一保存到桌面的「personal homepage完整最新版」文件夹

**后续约定**：
- 所有修改、新增文件都自动保存到此文件夹
- 每次修改后同步更新此文件夹内的所有相关文件
- 过程日志持续追加在此文件夹的 PROJECT_LOG.md 中

**验证结果**：
- ✅ 文件夹创建成功
- ✅ 7个文件全部复制到位
- ✅ index.html 为最新视频背景版本（14,653字节）
- ✅ bg-video.mp4 存在（370,060字节）
- ✅ 用户偏好已记录，后续自动保存

**下一步计划**：
1. 用户下载/访问此文件夹确认最新版本
2. 继续 V1 完善或进入 V2 多轮打磨阶段
3. 所有后续修改自动保存到此统一文件夹

---

## 任务 #006 — 微信朋友圈风格改版：封面图+亮度调高+点击放大

**日期时间**：2026-09-10
**阶段/版本**：阶段一 · V1.1
**任务内容**：
用户要求将页面改成微信朋友圈风格：
1. 将背景图亮度调高
2. 背景图设置成微信朋友圈封面那样（顶部横幅）
3. 点击封面图可放大查看

**设计变更**：
从之前的"全屏视频背景+底部文字"风格，改为"微信朋友圈个人主页"风格：

### 页面结构重构
- **顶部封面图**（.cover）：
  - 高度 320px（移动端 260px，桌面端 380px）
  - 背景图使用 bg.jpg（须佐能乎）
  - 亮度调高：`filter: brightness(1.25) contrast(1.05)`
  - 悬停时轻微放大并进一步提亮
  - 右上角"点击放大"提示标签
  - 底部渐变遮罩提升可读性

- **头像重叠**（.avatar-box）：
  - 位置：封面图右下角，向上重叠 44px
  - 尺寸：88×88px（移动端 72px，桌面端 100px）
  - 白色边框 + 阴影，微信朋友圈经典样式

- **个人信息区**（.profile）：
  - 姓名（24px 加粗）
  - 学校·专业·年级（15px 灰色）
  - 地点（14px 浅灰，带定位图标）

- **信息卡片**：
  - 背景从深色改为白色卡片（#fff）
  - 页面背景改为微信朋友圈浅灰色（#ededed）
  - 卡片圆角 12px，轻微阴影
  - 关于我：四宫格信息
  - 兴趣爱好：圆角标签
  - 联系方式：微信绿色图标（#07c160）

### 点击放大功能（Lightbox）
- 点击封面图弹出全屏黑色遮罩
- 图片居中显示，最大 92% 宽高
- 放大动画（zoomIn 0.3s）
- 右上角关闭按钮（×）
- 点击遮罩任意位置关闭
- ESC 键关闭
- 放大时保持亮度调高效果
- 打开时禁止页面滚动

### 技术实现
- CSS `filter: brightness(1.25)` 实现亮度调高
- CSS `object-fit: cover` 实现封面图裁剪
- 纯 JavaScript 实现 Lightbox（无依赖库）
- 图片使用相对路径引用（bg.jpg、avatar.jpg），确保本地打开正常
- 版本号更新为 V1.1

**遇到的问题**：
- 无重大问题
- 注意：图片路径从 CDN URL 改为相对路径，确保用户下载后本地打开能正常显示图片

**验证结果**：
- ✅ 封面图亮度调高，画面更明亮
- ✅ 微信朋友圈风格：顶部横幅封面+右下角重叠头像+浅灰背景+白色卡片
- ✅ 点击封面图放大查看正常
- ✅ 放大后点击关闭/ESC键关闭正常
- ✅ 头像重叠位置正确
- ✅ 信息卡片显示正常
- ✅ 响应式适配（移动端/桌面端）
- ✅ 邮箱链接可点击

**文件变更**：
- index.html：完全重写（从视频背景版改为微信朋友圈版）
- bg-video.mp4：保留在文件夹中（页面不再引用，备用）
- 其他文件不变

**下一步计划**：
1. 用户下载最新 zip 包，替换旧文件夹
2. 用户确认微信朋友圈风格效果
3. 继续 V1 完善或进入 V2 多轮打磨

---

## 任务 #007 — 封面改为动态视频 + 移除点击放大

**日期时间**：2026-09-10
**阶段/版本**：阶段一 · V1.2
**任务内容**：
用户反馈：
1. 封面图不是想要的，应该用那个动态视频（素描动漫女孩）
2. 不用点击放大功能
3. 保留微信朋友圈背景图功能

**变更内容**：

### 1. 封面从静态图片改为动态视频
- HTML：将 `<img src="bg.jpg" class="cover-img">` 替换为 `<video class="cover-video" autoplay muted loop playsinline><source src="bg-video.mp4"></video>`
- CSS：将 `.cover-img` 类名改为 `.cover-video`，保持亮度调高（brightness(1.25)）和悬停放大效果
- 视频属性：自动播放、静音、无限循环、移动端内联播放
- 视频源使用相对路径 `bg-video.mp4`，确保本地打开正常

### 2. 移除点击放大功能（Lightbox）
- 移除 CSS：`.lightbox`、`.lightbox.active`、`.lightbox img`、`.lightbox-close`、`@keyframes fadeIn`、`@keyframes zoomIn`
- 移除 HTML：`<div class="lightbox">` 整个元素
- 移除 JavaScript：整个 `<script>` 标签（包含封面点击、lightbox打开/关闭、ESC键关闭等逻辑）
- 移除封面的 `id="cover"` 和 `title="点击查看大图"`
- 移除右上角"点击放大"提示标签（`.cover-hint`）

### 3. 保留微信朋友圈风格
- 顶部横幅封面（高度 320px）
- 头像重叠在封面右下角（向上重叠 44px）
- 浅灰色页面背景（#ededed）
- 白色信息卡片
- 封面底部渐变遮罩
- 亮度调高（brightness 1.25）

**遇到的问题**：
- 无

**验证结果**：
- ✅ 封面为动态视频（素描动漫女孩），自动播放循环
- ✅ 视频亮度调高，画面明亮
- ✅ 微信朋友圈风格布局完整
- ✅ 头像重叠位置正确
- ✅ 无点击放大功能（点击封面无反应）
- ✅ 信息卡片显示正常
- ✅ 无JavaScript依赖，纯CSS+HTML

**文件变更**：
- index.html：封面改为视频，移除所有Lightbox代码
- bg-video.mp4：现在被页面引用（之前是备用）
- bg.jpg：保留在文件夹中（不再被页面引用，备用）
- 其他文件不变

**版本号**：V1.2

**下一步计划**：
1. 用户下载最新 zip 包，替换旧文件夹
2. 用户确认动态视频封面效果
3. 继续 V1 完善或进入 V2 多轮打磨

---

## 任务 #008 — 全屏视频背景版：亮度调高

**日期时间**：2026-09-10
**阶段/版本**：阶段一 · V1.3
**任务内容**：
用户上传了一个全屏视频背景版本的 index.html（非微信朋友圈版），要求将此版本的背景图（视频）亮度调高。

**用户上传版本分析**：
- 布局：全屏视频背景（Hero 100vh）+ 底部文字 + 右下角头像
- 背景：动态视频（bg-video.mp4，素描动漫女孩）
- 原亮度设置：无（视频原始亮度）
- 原遮罩透明度：顶部0.5 / 35%处0.3 / 65%处0.55 / 底部0.92

**变更内容**：

### 1. 视频亮度调高
- 给 `.bg-video` 添加 CSS filter：`filter: brightness(1.4) contrast(1.08)`
- brightness(1.4)：亮度提升 40%
- contrast(1.08)：对比度轻微提升 8%，避免提亮后画面发灰

### 2. 遮罩透明度调整
- 因视频亮度提升，相应降低暗色遮罩透明度，让更亮的画面透出来：
  - 顶部：0.5 → 0.3
  - 35%处：0.3 → 0.15
  - 65%处：0.55 → 0.35
  - 底部：0.92 → 0.8（保持底部文字可读性）

**遇到的问题**：
- 编辑时提示文件已被修改，重新读取后成功编辑

**验证结果**：
- ✅ 背景视频亮度明显提升，画面更明亮清晰
- ✅ 素描动漫女孩的线条细节更清楚
- ✅ 底部白色文字仍清晰可读（底部遮罩保持0.8）
- ✅ 视频自动播放、静音、循环正常
- ✅ 整体布局未变（全屏视频+底部文字+头像）

**文件变更**：
- index.html：基于用户上传版本，添加亮度filter并调整遮罩
- 其他文件不变

**版本号**：V1.3

**技术要点**：
- CSS `filter: brightness()` 是调整视频/图片亮度的标准方式
- brightness(1.0) 为原始亮度，>1.0 提亮，<1.0 变暗
- 提亮后通常需要配合 contrast 提升，避免画面发灰
- 提亮背景后应相应降低遮罩透明度，否则提亮效果被抵消

**下一步计划**：
1. 用户下载最新 zip 包，替换旧文件夹
2. 用户确认亮度效果
3. 如需进一步调整亮度，可修改 brightness 值

---

*后续任务将在此文件中继续追加记录。*

## V11.0 → V11.1 互动桌宠（2026-09-21）

### V11.0 新增
- 基于吸血鬼少女形象（pet.png，已用Pillow去白底+边缘羽化）制作互动桌宠
- 功能：左键拖拽（跑步姿势动画）、点击轮流触发 跳跃/压扁回弹/左右抖动、滚轮缩放(60~250px)
- 右键菜单9项：陪我聊聊天/摸摸头/喂吃的/让她走路(随机漫步)/让她睡觉(Zzz)/跟随鼠标/调整大小/置顶开关/退出程序
- 对话气泡置于角色左侧不遮挡，分类语录库；透明无边框，z-index置顶；退出后右下角🦇召唤

### V11.1 关键Bug修复
- **现象**：用户反馈桌宠"没有任何功能"（显示但点击/右键无反应）
- **根因**：桌宠JS的IIFE在 `<script>`(行2471起) 内立即执行，而桌宠HTML在 `</script>`(行3728) 之后才解析，getElementById 返回 null，命中 `if(!pet) return` 静默退出，所有事件监听均未绑定
- **修复**：IIFE 改为具名函数 initDesktopPet()，用 DOMContentLoaded（含readyState判断）确保DOM就绪后初始化
- **浏览器实测验证**：点击→jump动画+气泡✓；右键→9项菜单✓；摸摸头→squash+气泡✓；滚轮→120px变130px✓；真实拖拽→位置正确移动且留在视口✓

## V12.0 顶部导航 + 我的青春页面（2026-09-21）

### 新增功能
- 在"关于我"卡片右侧、反馈按钮旁边添加顶部导航："主页"和"我的青春"
- 导航样式：白色26px粗体文字，letter-spacing 2px，带文字阴影；当前页下方有粉色渐变下划线
- 高度与反馈按钮齐平（top:36px vs 反馈top:40px，实测差4px）
- 点击"主页"→ index.html（当前页，active高亮）
- 点击"我的青春"→ youth.html（新页面，active高亮）

### 新增 youth.html 页面
- 风格与主页一致：相同背景图(page-bg.jpg)、相同卡片样式、相同配色
- 页面标题"我的青春"，副标题"那些年，那些事，那些人"
- 📷 青春相册：3列网格照片墙，支持点击放大预览
- 🎬 青春影像：2列网格视频区，支持点击全屏播放
- 自动加载：照片命名 youth-1.jpg~youth-20.jpg，视频命名 youth-video-1.mp4~youth-video-10.mp4，放入文件夹自动显示
- 保留反馈按钮、顶部导航、互动桌宠（简化版）
- 响应式适配小屏幕

### 验证
- 浏览器实测：导航显示位置正确、高度齐平
- 主页→我的青春→主页 双向跳转正常
- youth.html 照片网格、视频区域、预览弹窗、桌宠均正常

## V12.1 青春相册照片浏览器（2026-09-23）

### 新增照片（20张，存 youth-photos/）
- 第14张原图作为封面 cover.jpg（"青春不可回收"黄色箱子）
- 第15张原图（竖版人像）放在第10张之后
- 实际展示顺序：cover → photo-1~10（原图1~10）→ photo-11（原图15）→ photo-12~19（原图11~13,16~20）
- 全部 WebP 转真正 JPEG（quality 88）

### 照片展示改造
- 从网格改为全屏渐显浏览器（75vh高）
- 点击任意处切换下一张（1.2s 交叉渐显动画）
- 右上角计数器 "x / 20"
- 左上角"封面"粉色标签（仅封面页显示）
- 底部缩略图条，点击可跳转到任意照片
- 最后一张点击自动循环回封面
- 后续加照片：往 youth-photos/ 放 photo-N.jpg（N从20开始）自动加载

### Bug修复
- 初版 slides/thumbs 数组未 push 元素，导致点击切换报错无反应，已修复

## V12.2 青春影像视频 + 移除计时器（2026-09-23）

### 改动
- 移除青春相册右上角的计数器（"x/20"）：删除HTML、CSS、JS引用
- 新增青春影像视频 youth-video-1.mp4（13.2秒，蓝色灯光走廊场景）
- 用 ffmpeg 提取第1秒帧作为封面 youth-video-1.jpg（poster），避免缩略图黑屏
- 视频点击后在预览弹窗全屏自动播放
- 第二格保留"添加视频"占位

### 后续加视频
- youth-video-N.mp4 + youth-video-N.jpg（封面，可选）放入文件夹自动加载

## V12.3 新增5张青春照片（2026-09-24）

- 3张新图插到 photo-10 之后（新 photo-11/12/13）
- 旧 photo-11~19 后移到 photo-14~22
- 2张新图加在最后（新 photo-23/24，含一张长竖版合照）
- 共 1封面 + 24照片 = 25张
- 全部WebP转JPEG

## V12.4 新增16张青春照片（2026-09-24）
- 16张新图插到第16张后面（新 photo-17~32）
- 原 photo-17~24 后移到 photo-33~40
- 共 1封面 + 40照片 = 41张

## V13.0 离线数字分身（2026-09-24）

### 功能
- 左下角悬浮头像按钮（avatar.jpg + 在线绿点），点击打开聊天面板
- 紫蓝渐变主题（#667eea→#764ba2），与桌宠（右下角）不冲突
- 纯前端离线问答，不联网、不接API、免费
- 知识库覆盖：身份/学校（深圳未来技术学院）/专业（智能医学工程）/年级（大一）/家乡（广州）/联系方式（邮箱）/四大爱好/音乐（8位歌手）/足球（曼联老特拉福德）/游戏（无畏契约）/美食/10个旅行城市/桌宠/青春页/笑话/兜底
- 关键词匹配取最长命中，多个回答随机
- 打字指示动效（三点跳动 + 延迟模拟真人）
- 7个快捷问题横滑按钮
- 回车/点按钮发送，初始欢迎语
- DOMContentLoaded 初始化（吸取桌宠教训）

### 注意
- 离线版只能答知识库范围内问题，兜底会引导话题
- 后续想加问答：在 index.html 的 KB 数组里加 {k:[关键词],a:[回答]}
